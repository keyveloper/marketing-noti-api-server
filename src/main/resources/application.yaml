spring:
  application:
    name: marketing-noti-api-server
  profiles:
    group:
      local: db-local
      prod: db-prod
      aws: db-aws
  config:
    import: "classpath:application-db.yaml"

server:
  port: 8085